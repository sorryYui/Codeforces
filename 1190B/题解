当最初的数据有超过两对重复的数时，先手必败。
当只有一对数时，先手需要自救，设该数为x,将x 自减 1，如果数组中也有x - 1，则先手必败；若没有，则交换操作权。
由于数组中不允许出现相同的数，即排序后先拿到0,1,2...n - 1的必败。
设sum为数组的和，故可操作空间为sum - 0 - 1...- n - 1 = k。
故sjf自救时:
  k % 2 == 0时必胜;
  sjf没有自救时:
  k % 2 == 1必胜。
