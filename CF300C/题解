因为要求一个长度为n的数中各个数为a或b时这个数是好数。
题目要求一个长度为n的数为好数，且这个数的各位数的和也为好数，一共由多少个数。
因为为好数，既可以枚举好数中max(a,b)的个数，检验它的和是否为好数。
记max(a,b)的个数为x，有
ans += C(n,x),x = 0,1,..n;且(max(a,b) * x + (n - x) * min(a,b))为好数。
时间复杂度:O(nlog(mod));
