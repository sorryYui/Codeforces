大型模拟题。
题目提供两个长度为len的字符串，分别为s和p；并且定义两个字符串在相同位置不同的字符的个数为f(s,p) = t;
要求构造一个字符串str,f(str,s) = f(str,t) = t;
可以找出p和s相同的字符x个，
  1.当x >= t时一定有解;
  2.当x < t时，对于s和p，需要x + 2 * (t - x) 个字符满足题意。如果2 * t - x > len时无解；否则有解。
可以贪心的构造字符串str。
