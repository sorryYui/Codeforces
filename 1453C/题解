首先先将每一个数的坐标保存起来。
由于要求三角形顶点的数字一样，至少一条边与表边平行，且可以改变表中的一个数，使三角形面积最大。
则枚举每一个坐标，以这个坐标为中心构造出最长平行边，之后在该数的最大最小值中找出该坐标对应的最长高。
即
  maxs[i] = max(maxs[i],a * h1,b * h2);
  a = max(n - 1 - x,x),h1 = max(y - miny,maxy - y);
  x,y为该点横，纵坐标，i为坐标上的值，miny,maxy为坐标上的值得最小y坐标和最大y坐标。
  b =  a = max(n - 1 - y,y),h1 = max(x - minx,maxx - x);
  同上类似。
